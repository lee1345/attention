<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="fs.four.human.freeBoard.dao.FreeBoardDAO">

    <!-- 전체 주소록 조회 -->
    <select id="getAllFreeBoard" resultType="FreeBoardVO">
        SELECT
        B_ID as b_Id,
        B_TITLE as b_Title,
        B_CONTENT as b_Content,
        B_WRITER as b_Writer,
        B_GROUP as b_Group,
        B_CATEGORY as b_Category,
        B_CREATED_DATE as b_CreatedDate,
        B_UPDATED_DATE as b_UpdatedDate
        FROM BOARD
    </select>

    <!-- 검색 기능 -->
    <select id="searchFreeBoard" resultType="FreeBoardVO">
        SELECT
        B_ID as b_Id,
        B_TITLE as b_Title,
        B_CONTENT as b_Content,
        B_WRITER as b_Writer,
        B_GROUP as b_Group,
        B_CATEGORY as b_Category,
        B_CREATED_DATE as b_CreatedDate,
        B_UPDATED_DATE as b_UpdatedDate
        FROM BOARD
        WHERE LOWER(${category}) LIKE '%' || LOWER(#{query}) || '%' <!-- 검색 필터 적용 -->
    </select>

        <!-- 새로운 게시판 데이터 등록 -->
    <insert id="createFreeboard">
        INSERT INTO BOARD (
        b_id, b_title, b_content, b_writer, b_group, b_category, b_created_date
        ) VALUES (
        freeboard_seq.NEXTVAL, -- 자유게시판 시퀀스
        #{b_Title},
        #{b_Content},
        #{b_Writer},
        'F',
        #{b_Category}, -- 자유게시판 카테고리
        SYSDATE
        )
    </insert>

</mapper>
