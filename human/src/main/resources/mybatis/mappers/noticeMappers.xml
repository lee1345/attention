<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="fs.four.human.notice.dao.NoticeDAO">

    <!-- 전체 공지사항 조회 -->
    <select id="getAllNotice" resultType="NoticeVO">
        SELECT
        B_ID as b_Id,
        B_TITLE as b_Title,
        B_CONTENT as b_Content,
        B_WRITER as b_Writer,
        B_GROUP as b_Group,
        B_CATEGORY as b_Category,
        B_CREATED_DATE as b_CreatedDate,
        B_UPDATED_DATE as b_UpdatedDate
        FROM BOARD
        WHERE b_group = 'N' -- 공지사항만 조회
        ORDER BY b_created_date DESC
    </select>

    <!-- 검색 기능 -->
    <select id="searchNotice" resultType="NoticeVO">
        SELECT
        B_ID as b_Id,
        B_TITLE as b_Title,
        B_CONTENT as b_Content,
        B_WRITER as b_Writer,
        B_GROUP as b_Group,
        B_CATEGORY as b_Category,
        B_CREATED_DATE as b_CreatedDate,
        B_UPDATED_DATE as b_UpdatedDate
        FROM BOARD
        WHERE b_group = 'N' -- 공지사항만 검색
        AND LOWER(${category}) LIKE '%' || LOWER(#{query}) || '%' -- 대소문자 구분 없이 검색
        ORDER BY b_created_date DESC
    </select>

    <!-- 새로운 공지사항 데이터 등록 -->
    <insert id="createNotice">
        INSERT INTO BOARD (
        b_id, b_title, b_content, b_writer, b_group, b_category, b_created_date, b_created_id, b_updated_date, b_updated_id
        ) VALUES (
        notice_seq.NEXTVAL,
        #{b_Title},
        #{b_Content},
        #{b_Writer},
        'N',
        NULL, -- 카테고리는 공지사항에서는 사용하지 않음
        SYSDATE,
        NULL, -- b_created_id
        NULL, -- b_updated_date
        NULL  -- b_updated_id
        )
    </insert>

</mapper>
